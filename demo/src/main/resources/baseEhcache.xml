<ehcache>

    <!--
        磁盘存储:将缓存中暂时不使用的对象,转移到硬盘,类似于Windows系统的虚拟内存
        path:指定在硬盘上存储对象的路径
        path可以配置的目录有：
            user.home（用户的家目录）
            user.dir（用户当前的工作目录）
            java.io.tmpdir（默认的临时目录）
            ehcache.disk.store.dir（ehcache的配置目录）
            绝对路径（如：d:\\ehcache）
        查看路径方法：String tmpDir = System.getProperty("java.io.tmpdir");
     -->
    <diskStore path="java.io.tmpdir"/>

    <!--
        defaultCache:默认的缓存配置信息,如果不加特殊说明,则所有对象按照此配置项处理
        maxElementsInMemory:设置了缓存的上限,最多存储多少个记录对象
        eternal:代表对象是否永不过期 (指定true则下面两项配置需为0无限期)
        timeToIdleSeconds:最大的发呆时间 /秒
        timeToLiveSeconds:最大的存活时间 /秒
        overflowToDisk:是否允许对象被写入到磁盘
        说明：下列配置自缓存建立起600秒(10分钟)有效 。
        在有效的600秒(10分钟)内，如果连续120秒(2分钟)未访问缓存，则缓存失效。
        就算有访问，也只会存活600秒。
     -->

    <!--  大对象，实时性高，更新率高，10分钟存活时间 -->
    <cache name="cache:big_up_up" timeToLiveSeconds="600"/>

    <!--  大对象，实时性高，更新率低 1小时有效期-->
    <cache name="cache:big_up_low" timeToLiveSeconds="3600"/>

    <!--  大对象，实时性低，更新率高 1天存活时间 -->
    <cache name="cache:big_low_up" timeToLiveSeconds="86400"/>

    <!--  大对象，实时性低，更新率低 10天存活时间-->
    <cache name="cache:big_low_low" timeToLiveSeconds="864000"/>


    <!--  小对象，实时性高，更新率高，1分钟有效期  -->
    <cache name="cache:small_up_up" timeToLiveSeconds="60"/>

    <!--  小对象，实时性高，更新率低 10分钟有效期 -->
    <cache name="cache:small_up_low" timeToLiveSeconds="600"/>

    <!--  小对象，实时性低，更新率高 1天存活时间 -->
    <cache name="cache:small_low_up" timeToLiveSeconds="86400"/>

    <!--  小对象，实时性低，更新率低 5天存活时间 -->
    <cache name="cache:small_low_low" timeToLiveSeconds="43200"/>


    <!--  10秒分钟  -->
    <cache name="s10" timeToLiveSeconds="10"/>

    <!--  30秒分钟  -->
    <cache name="s30" timeToLiveSeconds="30"/>

    <!--  1分钟  -->
    <cache name="m1" timeToLiveSeconds="60"/>

    <!--  10分钟  -->
    <cache name="m10" timeToLiveSeconds="600"/>

    <!--  30分钟  -->
    <cache name="m30" timeToLiveSeconds="1800"/>

    <!-- 1小时-->
    <cache name="h1" timeToLiveSeconds="3600"/>

    <!-- 10小时-->
    <cache name="h10" timeToLiveSeconds="36000"/>

    <!-- 1天-->
    <cache name="d1" timeToLiveSeconds="86400"/>

    <!-- 5天-->
    <cache name="d5" timeToLiveSeconds="432000"/>

    <!-- 10天-->
    <cache name="d10" timeToLiveSeconds="864000"/>

    <!-- 30天-->
    <cache name="d30" timeToLiveSeconds="2592000"/>

    <!-- 1年-->
    <cache name="y1" timeToLiveSeconds="31104000"/>

    <!-- 永久 1特殊秒 -->
    <cache name="ALWAYS" timeToLiveSeconds="1"/>

</ehcache>
